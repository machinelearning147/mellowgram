import React,{useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../utils/auth';export default function Auth(){const{signInEmail,signInGuest}=useAuth();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const nav=useNavigate();const handleEmail=async(e)=>{e.preventDefault();await signInEmail({email,password:pw});nav('/');};const handleGoogle=()=>alert('Google Sign-in stub');return(<div className='p-4 space-y-4'><h2 className='text-lg font-semibold'>Sign in</h2><form onSubmit={handleEmail} className='space-y-3'><input className='w-full p-3 rounded-xl border' placeholder='Email' value={email} onChange={(e)=>setEmail(e.target.value)}/><input type='password' className='w-full p-3 rounded-xl border' placeholder='Password' value={pw} onChange={(e)=>setPw(e.target.value)}/><button className='w-full py-3 rounded-xl bg-gradient-to-r from-[var(--accent-lav)] to-[var(--accent-peach)]'>Sign in with Email</button></form><button className='w-full py-3 rounded-xl border' onClick={handleGoogle}>Sign in with Google</button><div className='text-center text-sm text-[var(--text-muted)]'>or</div><button className='w-full py-3 rounded-xl bg-white border' onClick={()=>{signInGuest();nav('/');}}>Continue as Guest</button></div>);}