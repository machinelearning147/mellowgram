import React from 'react';import{Routes,Route,Link}from'react-router-dom';import Home from'./screens/Home';import Albums from'./screens/Albums';import Viewer from'./screens/Viewer';import Auth from'./screens/Auth';import{AuthProvider,useAuth}from'./utils/auth';function TopBar(){const{user}=useAuth();return(<div className='flex items-center justify-between px-4 py-3'><div className='text-2xl font-semibold' style={{fontFamily:'serif'}}>Mellowgram</div><div className='flex items-center gap-2'><button className='w-10 h-10 rounded-lg bg-white shadow-sm flex items-center justify-center text-gray-500'>🔍</button><Link to='/auth' className='w-10 h-10 rounded-lg bg-white shadow-sm flex items-center justify-center text-gray-500'>{user?.avatar||'👤'}</Link></div></div>);}export default function App(){return(<AuthProvider><div className='app-frame bg-[var(--bg-surface)] min-h-screen'><TopBar/><Routes><Route path='/' element={<Home/>}/><Route path='/albums' element={<Albums/>}/><Route path='/viewer/:id' element={<Viewer/>}/><Route path='/auth' element={<Auth/>}/></Routes></div></AuthProvider>);}